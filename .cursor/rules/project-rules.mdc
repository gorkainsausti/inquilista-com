---
alwaysApply: true
---
Perfecto. Voy a reescribir tu archivo de rules para que sea coherente con el MVP de inquilista, con lo que hemos decidido de:
	•	MVP first
	•	Next.js + Hono
	•	Cloudflare-agnóstico
	•	Nada de sobre-ingeniería
	•	Cursor como copiloto (no arquitecto)
	•	SEO + SSR + backend serio, pero simple

👉 Mantengo lo bueno de tu rule actual y elimino / rebajo lo que ahora te estorba (three.js, exceso de patterns, UI innecesaria, etc.).

Abajo tienes el RULE FINAL, listo para sustituir el actual.

⸻

✅ RULES PARA CURSOR — INQUILISTA MVP

---
alwaysApply: true
---

You are an expert software engineer focused on building pragmatic MVPs for public-facing, SEO-driven web platforms with a real backend.

The project is inquilista, a civic web platform with:
	•	Public SEO-first pages
	•	Authenticated user submissions
	•	A simple backend for case management
	•	A strict MVP mindset (no overengineering)

⸻

🎯 Core Principles (NON-NEGOTIABLE)
	•	MVP first: implement only what is required to make the product usable once.
	•	Prefer clarity over abstraction.
	•	Avoid speculative architecture.
	•	Code must be easy to delete or extend later.
	•	Vendor-agnostic by design (Cloudflare is only a deployment target).

⸻

🧱 Stack Assumptions
	•	Frontend: Next.js (App Router, TypeScript)
	•	Backend: Hono (TypeScript)
	•	Database: PostgreSQL (via Prisma)
	•	Auth: email + password (JWT, cookie-based)
	•	Styling: minimal Tailwind CSS
	•	Deployment (initial): Cloudflare Pages + Workers

Do NOT introduce other frameworks unless explicitly requested.

⸻

🧠 General Coding Guidelines
	•	Use TypeScript everywhere.
	•	Prefer interfaces over types.
	•	Avoid enums; use string unions or maps.
	•	Use functional, declarative programming.
	•	Avoid classes unless absolutely necessary.
	•	Prefer simple functions over abstractions.
	•	Do not introduce design patterns unless solving a real problem.

Naming:
	•	Descriptive variable names with auxiliary verbs (isLoading, hasError)
	•	Lowercase with dashes for directories (components/login-form)
	•	Favor named exports

⸻

📁 Project Structure Philosophy
	•	Flat > nested.
	•	Fewer files > many small files.
	•	A file should be understandable without jumping across the repo.

Avoid:
	•	DDD
	•	Clean Architecture
	•	CQRS
	•	Event sourcing
	•	Hexagonal layers

⸻

🧪 Error Handling
	•	Handle invalid states early (guard clauses).
	•	Use early returns.
	•	Avoid deeply nested conditionals.
	•	Do not add complex error hierarchies.
	•	Expected errors:
	•	return structured error objects
	•	Unexpected errors:
	•	let them throw and be caught by error boundaries

No logging frameworks unless requested.

⸻

⚛️ React / Next.js Rules
	•	Use function declarations, not const, for components.
	•	Use Server Components by default.
	•	Mark Client Components explicitly with "use client" only when required.
	•	Keep JSX declarative and minimal.
	•	No premature optimization.
	•	No UI libraries unless explicitly requested (default: basic HTML + Tailwind).

Forms:
	•	Start with uncontrolled or simple controlled forms.
	•	No form libraries unless needed.
	•	Validation can be basic (required fields only).

SEO:
	•	Use semantic HTML.
	•	Use Next.js metadata API.
	•	Avoid client-side rendering for public pages.

⸻

🧵 Styling (Tailwind)
	•	Minimal utility usage.
	•	No design systems.
	•	No animations unless necessary.
	•	Focus on readability, not aesthetics.

⸻

🌐 Backend (Hono)
	•	One API service.
	•	One responsibility: serve data to frontend.
	•	No microservices.
	•	No queues.
	•	No background jobs in MVP.
	•	Use standard HTTP status codes.
	•	Keep handlers small and readable.

Avoid:
	•	Cloudflare KV
	•	Durable Objects
	•	Vendor-specific APIs

⸻

🗄 Database (Prisma)
	•	Keep schema minimal.
	•	No premature normalization.
	•	UUIDs as primary keys.
	•	Explicit relations only when necessary.
	•	No triggers, no stored procedures.

⸻

🚫 Explicitly Forbidden Unless Asked
	•	Over-engineered folder structures
	•	Reusable “frameworks” inside the repo
	•	Generic abstractions “for the future”
	•	AI features
	•	Complex permissions systems
	•	Analytics pipelines
	•	Internationalization
	•	Background workers
	•	Fancy admin UIs

⸻

🧠 Cursor-Specific Instructions
	•	Generate code, not explanations.
	•	Do one task at a time.
	•	Do not refactor unrelated code.
	•	Do not “improve” architecture.
	•	Do not add features beyond the prompt.
	•	If unsure, choose the simplest possible implementation.

⸻

🧭 Mental Model to Follow

This is a civic MVP that must work, be understandable, and earn trust —
not a showcase of technical sophistication.